# ComfyUI WebUI Prompt Bridge v0.4.26

这是一次小型问题修复更新：

- 收藏列表不再只显示前 260 条。
- 收藏项可直接在收藏列表中删除，无需先加入提示词框。
- 迁移 WebUI 后可重新自动检测常见的 `AI/Tools` 安装目录；也可直接粘贴 `models/Lora` 目录完成接入。

更新后请重启 ComfyUI，并在浏览器中按 `Ctrl+F5` 强制刷新。
